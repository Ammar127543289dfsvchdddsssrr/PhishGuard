# المساهمة في PhishGuard

شكراً لاهتمامك بالمساهمة في المشروع.

قبل إرسال أي تعديل، شغّل الاختبارات:

```bash
python -m unittest discover -s tests -v
```

يجب أن تكون التعديلات واضحة وقابلة للتفسير، وألا تضيف مكتبات خارجية إلى النواة دون مبرر ومراجعة. عند إضافة قاعدة كشف جديدة، أضف اختباراً يوضح الحالة الإيجابية والحالة التي لا ينبغي أن تتأثر بها القاعدة.

استخدم رسائل Commit قصيرة وواضحة، واشرح في Pull Request المشكلة التي يعالجها التعديل، وطريقة اختباره، وأي أثر أمني أو تغيّر في المخرجات.
